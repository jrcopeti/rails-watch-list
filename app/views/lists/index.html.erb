<h1>My awesome watch list</h1>
<br>
<ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, list_path(list) %></li>
    <%= link_to "delete this list", list_path(list), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>

<% end %>
</ul>
<br>
<%= link_to "Create a new list", new_list_path %>
